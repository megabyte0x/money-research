# Package 08 handoff — 15 September 2026

## Implemented in owned files

- `src/search.js`: exact alias groups for QE, CBDC, UTXO, proof of work, BIP and Federal Reserve. Short aliases require word boundaries. Existing search URL state, volume filters, section grouping and Triffin ranking are preserved.
- `src/features/discovery/catalog.js`: explicit role/category metadata for every manifest ID, short titles, topic tags, a topic/takeaway eligibility function that includes Bitcoin 10–13, and five ordered reading paths. Path times are computed from the current manifest's word counts at 230 words/minute. Missing chapter IDs throw rather than silently disappearing.
- `src/features/discovery/canonical.js`: deliberate neutral proposals for all seven duplicate glossary IDs (fiat money, legal tender, petrodollar, Triffin dilemma, unit of account, GENIUS Act and stablecoin). Each has a stable ID, alias field, example, related IDs, topics and chapters. Dated or disputed claims are separated into review notes. Proposals are labeled `proposed`; unique source terms are labeled `source-only`. These labels are editorial state, not evidence sign-off.
- `src/features/discovery/DiscoveryViews.jsx` and `discovery.css`: isolated search, glossary, research index, paths and synthesis components. They use a single `v` prop and real anchors; they do not fetch the index or parse routes. Search and glossary accept the currently derived App props. Index, paths and synthesis need coordinator props and routes before they can render in the site.
- `workstreams/08/SYNTHESIS-PROPOSAL.md`: withheld cross-volume short-reading copy, approximately five to six minutes at 230 words/minute. Its 17 chapter links resolve against the current manifest. It avoids live numbers and marks unresolved evidence. It has not been approved by the editors or evidence package.
- `tests/discovery.test.mjs`: verifies 44-record coverage, explicit roles, late Bitcoin takeaway eligibility, path resolution/time, duplicate-term coverage and alias collision/URL behavior.

## Coordinator wiring requests

1. Import `SearchView` and `GlossaryView` into `App.jsx` for the existing views, passing current `v`. Keep route, URL restoration, inline rendering and focus in App. Existing page headings in App should be replaced by these components only when the shell integration is ready.
2. For the research index, derive `v.indexRows = researchCatalog(manifest).map(row => ({ ...row, href: this.href(row.article) }))`, plus `indexVolume`, `indexRole`, `onIndexVolume` and `onIndexRole` state/callbacks. Route `#/research` remains available. Check mobile filters and browser Back after integration.
3. Add a `#/paths` route with `v.paths = learningPaths(manifest)` and `v.hrefForArticle = article => this.href(article)`. The path IDs are stable route anchors; validate section/scroll behavior before exposing links.
4. After editors 02–04 and evidence 01 accept the seven canonical proposals, use `canonicalGlossary` in the shared content-model build seam to replace first-duplicate wins. Keep claim-specific and dated notes linked to accepted claims/sources rather than appending them to general definitions. Update App's inline glossary map from the accepted canonical set and check the Triffin/QE rankings again.
5. Replace the 01–09 takeaway filter in `App.jsx` with explicit `takeawayCandidates(manifest, blocks)` when accepted chapter summaries exist. Do not publish the current concatenated sections as the five-minute synthesis. `SynthesisView` needs reviewed `v.synthesis` data with paragraphs and links; source URLs should be shown on source pages in keeping with the user preference, without author/editor or corrections-contact UI.
6. Pass accepted evidence/source relations from the generated model through App props. The discovery components should display only accepted claim relationships. Canonical proposals and synthesis text must not imply that every chapter assertion is accepted.

## Verification performed

`node --test tests/discovery.test.mjs tests/search.test.mjs` passed (2 test files). An esbuild bundle of `DiscoveryViews.jsx` passed syntax/transformation checking. A script checked that all 17 chapter links in the synthesis proposal match current manifest slugs. No browser checks, responsive inspection, moderated reader sessions, editorial sign-off, external source validation or full integrated build were performed by package 08.

## Remaining dependencies and unknowns

The seven duplicate definitions need volume-editor conflict resolution and accepted evidence locators for their dated notes. The short synthesis needs paragraph-level editorial approval and copy conversion to accepted model data. Approved article summaries and topic/volume variants are not yet available to package 08. App/model/shared registries and generated output remain coordinator-owned. Route and browser Back checks depend on coordinator integration; accessibility and mobile verification depend on package 10.

## Bounded editorial second pass — 15 September 2026

After editors 02–04 approved conceptual reader summaries for all 32 topic chapters, package 08 cross-checked the seven canonical proposals and the short synthesis against those packets and corrected glossary/chapter text. [CANONICAL-REVIEW.md](CANONICAL-REVIEW.md) gives an accept/qualify/withhold recommendation for each duplicate with the precise source definitions and evidence limits, plus a paragraph-level synthesis copy/routing matrix. The notes in `canonical.js` were updated to distinguish existing scoped locators from missing law/market evidence; the synthesis preamble and reserve-observation caveat were revised. The synthesis remains withheld as new cross-volume copy, and GENIUS law detail remains withheld pending statutory section locators. Tests for discovery/search passed, all 17 synthesis chapter links still matched manifest slugs, and `git diff --check` passed for the three owned review files. No browser, live statute retrieval, full fact-check, shared-file edit or editorial sign-off was performed in this second pass.

## Final bounded canonical pass — 15 September 2026

Gold-07, After-12 fiat and Bitcoin-15/06/07/10/11/12 were narrowed after the preceding review. The enacted GENIUS Act was checked at the GPO Public Law 119-27 text and editor 04's exact §2/§4/§7(e)/§20 locators. [CANONICAL-REVIEW.md](CANONICAL-REVIEW.md) now recommends six conceptual IDs as eligible for coordinator/editor acceptance (`fiat-money`, `legal-tender`, `petrodollar`, `triffin-dilemma`, `unit-of-account`, `stablecoin`) while their dated/named notes remain withheld. `genius-act` has a located enacted-framework core but remains withheld from release: After-12 still publishes cash/Treasury-only and generic licensing/audits wording, and present effectiveness and named issuer approvals have not been established. Editor 03 should correct After-12; source 01 should reconcile the statutory source/claim before public integration. The revised `canonical.js` notes and GENIUS core remain `proposed`; no shared model or App edit was made. The `#/paths` route correction in the review was preserved. Owned discovery/search tests passed, 17 synthesis chapter links resolved, and diff whitespace check passed. No verbatim synthesis publication or browser check was performed.

## GENIUS source-page follow-through — 15 September 2026

Editor 03 subsequently corrected After-12's GENIUS definition to the enacted Public Law 119-27 scope, and editor 04 added the official GPO statute with exact sections to Bitcoin-16's primary-document sources. The preceding After-12 contradiction is therefore **resolved**; the prior paragraph records the state at that checkpoint. The current [CANONICAL-REVIEW.md](CANONICAL-REVIEW.md) recommends all seven conceptual cores for qualified coordinator/editor copy acceptance while keeping their `review` state `proposed`. `canonical.js` now routes GENIUS to the directly relevant `bitcoin-11` chapter instead of incidental `bitcoin-06`. Shared-model publication, source 01 statutory-record reconciliation, exact current effectiveness, implementing rules, named issuer approval/compliance, and route/browser review are still outstanding. No synthesis text was published or marked accepted.

## Gold exact-copy qualification handoff — 15 September 2026

Gold editor 02's review of the exact `definition` and `example` strings requested three final scopes. `fiat-money` now starts “A state-issued currency or monetary unit...” so the core does not absorb bank deposits or private tokens. `triffin-dilemma` now names foreign **official** dollar claims held as reserves; its example is a conditional relative-growth possibility, not a measured stock ratio. `unit-of-account` now says “A unit ... in a specified arrangement” so one unit is not implied for every obligation or economy. [CANONICAL-REVIEW.md](CANONICAL-REVIEW.md) records the residual limits. All seven cores remain `proposed`; the discovery owner is releasing the single-writer lease on `canonical.js` for coordinator acceptance/model integration after the final cross-volume copy check. Owned discovery/search tests and diff check were rerun; no shared model, App or synthesis publication was changed.

## Accepted-core glossary UI addendum — 15 September 2026

The coordinator subsequently accepted the seven conceptual cores through an explicit allowlist and connected them in `src/content-model.js`, as recorded in [00/CANONICAL-ACCEPTANCE.md](../00/CANONICAL-ACCEPTANCE.md). This supersedes the prior `proposed` checkpoint for those exact seven IDs; source-only terms remain unchanged and dated/named claims remain separate. The discovery owner changed only `src/features/discovery/DiscoveryViews.jsx` and `discovery.css` to display a short illustration, related canonical-term links and relevant chapter links for rows with `review === 'accepted'`. The view does not render the canonical `note` field and does not display the extra material for `source-only` rows.

Required App prop shape: `v.glossaryRows` contains `{ id, term, review, defEl, exampleEl, relatedLinks: [{ id, label, href }], chapterLinks: [{ id, label, href }] }`. App should populate `exampleEl` and links only for accepted rows, derive term links from accepted canonical IDs and chapter links from reviewed manifest destinations, and keep route/inline rendering in App. A component-level SSR check showed extras once for the accepted mock row and none for a source-only mock row; owned tests and JSX transformation passed. No shared build, browser flow, synthesis publication or acceptance of current GENIUS effective date/named issuer status was performed by package 08 in this UI addendum.

## Revised withheld synthesis handoff — 15 September 2026

The discovery owner reconciled the exact [Gold 02](../02/synthesis-gold-review.md), [After Gold 03](../03/SYNTHESIS-REVIEW.md) and [Bitcoin 04](../04/SYNTHESIS-REVIEW.md) paragraph decisions into one revised [SYNTHESIS-PROPOSAL.md](SYNTHESIS-PROPOSAL.md). Its ten-row reconciliation matrix names verbatim cross-volume copy approvals and evidence/route decisions still needed. It removes the provisional comparison-view link and unsupported measured distributional/outcome phrasing, preserves payment-versus-settlement, official Triffin, reserve-denominator, bank-deposit, validator, adoption-status and intermediary-claim qualifications, and links readers to the full Bitcoin monetary-regimes chapter instead. The reading portion is about 1,400 words (~six minutes at 230 words/minute), so a final five-minute edition still needs careful shortening after approval. All 18 chapter links matched current manifest slugs; paths/research routes exist in App, and no compare link is exposed. `git diff --check` passed for the owned proposal. The revised synthesis remains **withheld**; no App/model, volume prose, shared evidence, build or browser change was performed by this editorial pass.

## Five-minute condensation handoff — 15 September 2026

The reading portion of [SYNTHESIS-PROPOSAL.md](SYNTHESIS-PROPOSAL.md) is now **1,117 visible words**, or **4.86 minutes** at 230 words/minute. This supersedes the earlier six-minute checkpoint above. All ten roles and 18 topical chapter destinations were retained; the revised reconciliation table calls out each shortened sentence needing the responsible editor's verbatim reapproval. Conditional Triffin, regional Gold roles, reserve valuation/denominators, payment versus settlement, Bitcoin validation versus credit, custody/claim distinctions and law/source caveats remain explicit. The synthesis is still withheld; no shared App/model, volume prose, evidence registry or publication was changed.

## Exact-editor-recheck synthesis disposition — 15 September 2026

The final owned copy edit in [SYNTHESIS-PROPOSAL.md](SYNTHESIS-PROPOSAL.md) applies the Gold 02, After Gold 03 and Bitcoin 04 condensed rechecks to all ten paragraphs. Paragraphs 1, 2, 5, 3 and 9 combine scopes or introduce a sentence break, so they need fresh verbatim cross-volume review; paragraphs 3 and 9 are explicitly withheld as full paragraphs because the editors' exact proposals differ. Paragraphs 4, 6, 7, 8 and 10 carry their responsible editor's conceptual acceptance/qualified wording, but they are **not** approved public synthesis: evidence 01 must reconcile dated chronology, protocol/current-use, crisis and claim/source relationships, and coordinator 00 must accept routes and final copy. The IMF historical text supports the August 1971 official-dollar conversion distinction and separate par-value reform; it does not establish a measured Triffin dollar-claims/US-gold ratio. No live return, reserve share, country adoption, named stablecoin solvency or comparative winner was added. The draft is about 1,205 visible words (5.24 minutes at 230 words/minute), and it remains withheld rather than being passed as `v.synthesis` to App. Full chapter destinations are topical research paths, not evidence sign-off. Files changed: this handoff and the owned synthesis proposal; no shared model/App/manifest, tests, builds or browser checks were touched during this edit. Source/evidence reconciliation, exact combined-copy recheck and package 10 navigation/Back checks remain deferred dependencies.

## Approved chapter-answer volume/topic views — 15 September 2026

The owned `catalog.js`, `DiscoveryViews.jsx` and `discovery.css` now support a separate browse view based on the **32 editor-approved chapter answers** already held in `public/content/article-metadata.json`. `approvedSummaryCatalog(manifest, articleMetadata)` selects only manifest rows whose metadata has `status === 'approved'` and `summary.answer`; it does not synthesize a new conclusion or elevate the linked chapter's dated assertions. Thirty-one rows are topic chapters; `after-10` is an approved reference-table answer and is labeled as such. After Gold's approved metadata has answer-only summaries, so the view uses the short chapter title as their heading rather than inventing a question. When `v.synthesis` is absent, `SynthesisView` shows those answers verbatim with volume and topic filters, chapter links, any metadata evidence/uncertainty fields, a live result count and an empty state. The disputed full synthesis remains withheld. Coordinator App wiring should pass `v.summaryRows = approvedSummaryCatalog(st.manifest, st.articleMetadata).map(row => ({ ...row, href: this.href(row.article) }))`, `v.summaryVolume`, `v.summaryTopic`, and `v.onSummaryVolume`/`v.onSummaryTopic` callbacks; it should use this view for `#/takeaways` instead of the existing concatenated section extraction. App owns route/filter state and URL/Back restoration. This edit touched only owned discovery files and this handoff; no tests, build or browser checks were run. Package 10 should later check all 32 approved answers appear, volume/topic filtering, direct chapter links, keyboard/mobile usability and Back restoration.

## Published metadata contract correction — 15 September 2026

`approvedSummaryCatalog` now accepts both forms of metadata. A raw `public/content/article-metadata.json` array still requires `status === 'approved'` and a stable `id` for each row. The generated `st.articleMetadata` object is keyed by article ID and contains `{ summary, nextSteps }` without `id` or `status`; `src/article-metadata.js` has already enforced approval while generating that object, so the discovery selector uses the object key and checks for a summary answer. It also requires the published `nextSteps` array and rejects any explicit non-approved status on an object row. The App prop request above remains `approvedSummaryCatalog(st.manifest, st.articleMetadata)` and now matches the published content model. Only owned `catalog.js` and this handoff changed in this correction. No tests, builds or browser checks were run.

## D05 inline-link rendering seam — 15 September 2026

The isolated `SynthesisView` now accepts an optional `v.renderSynthesisParagraph(paragraph, index)` callback. If the coordinator later accepts cross-volume copy, App can pass its existing safe inline Markdown renderer for the approved paragraph strings, preserving chapter/source links as React elements. Without the callback, the view retains plain text rendering; the approved chapter-answer fallback is unchanged. This seam publishes no copy by itself: the withheld `SYNTHESIS-PROPOSAL.md` remains outside the public bundle/model and `v.synthesis` must stay absent until exact editor/evidence approval. Only owned `DiscoveryViews.jsx` and this handoff changed. Tests, build and browser QA remain deferred to package 10.

## Exact D05 publication packet — 15 September 2026

[PUBLICATION-COPY-PACKET.md](PUBLICATION-COPY-PACKET.md) is an owned review artifact containing all ten current withheld paragraphs **verbatim**, their direct chapter/primary-source links, the narrow accepted claim/source IDs and exact locators per paragraph, volume-editor scope, unsupported assertions and a paragraph-level eligibility verdict. It reconciles latest Gold 02, After Gold 03, Bitcoin 04, evidence 01, coordinator 00 and five scoped C09 cells without claiming those cells rank arrangements. The paragraph text is publication *data for review*, not a public-bundle promotion: all ten remain withheld as one synthesis until coordinator 00 accepts exact claim relationships and the cross-view repetition fixes, package 09 keeps broad V01 comparisons bounded, and package 10 runs route/Back/browser QA after edit freeze. Current `SynthesisView` preserves the editor-approved volume/topic short-answer filters even when a future accepted `v.synthesis` is supplied and its `renderSynthesisParagraph` seam can render safe inline links. I did not edit public JSON, App, content model, the manifest or generated output, and did not write/run tests, build or browser checks. High-risk linked-chapter/glossary/timeline repetitions are identified as propagation holds rather than being certified by a destination link.
