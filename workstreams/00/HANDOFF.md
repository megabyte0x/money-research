# 00 integration handoff — staged local preview

The coordinator owns the shared source, claim, observation, article metadata, build, App and global status seams. The active checkout is local commit `7c9939c` with pre-existing uncommitted implementation across all packages; no reset or clean was performed. The four-record draft `public/content/sources.json` and `src/sources.js` were preserved and integrated rather than overwritten. `public/content/` remains the active editorial source, while `project/` and sibling research directories remain provenance snapshots. Stable article IDs, routes and numeric aliases remain in the manifest.

The evidence, reader, discovery and comparison contracts are recorded in `CONTRACTS.md`, `UI-BOUNDARIES.md`, `CANONICAL-ACCEPTANCE.md` and `COMPARISON-ACCEPTANCE.md`. Editors own their volume prose; package 05 owns timeline rows and durable IDs; package 08 owns synthesis and discovery; package 09 owns comparison proposals. Package 10 takes the consolidated validation pass after the edit freeze.

New shared integration: accepted claims with explicit dependent article IDs now produce `articleClaims` in the generated content model. Both React and static article views display the assertion, narrow scope, publisher URL and exact supporting locator under “Sources for reviewed claims”. The notice states that these locators do not certify an entire chapter. Dated observation locators remain in the separate dated-evidence section. This exposes the existing accepted E26/E29/E31 chapter relationships without promoting review-state proposals or unsupported whole-volume conclusions. Testing and generated-output refresh are deferred to package 10 after packages 05, 08 and 09 return their edits.

Editors 02–04 approved exact R08 packets for all 32 article-metadata records. The shared file now holds 32 questions, 32 answers, 32 sets of three-to-five scoped takeaways and 32 evidence/limits fields, alongside the previously approved two typed next steps per chapter. The metadata validator now requires these fields for every approved record. Gold and After Gold mixed source/limits paragraphs use `evidenceAndUncertainty` so they are not recast as verified fact badges; Bitcoin has separate evidence and uncertainty strings. The reader and static outputs render these fields. Copy approval remains distinct from full chapter evidence sign-off, and generated-output/testing checks are deferred until the final edit freeze.

`CLAIM-MATRIX.md` records scoped E01–E30 dispositions. It is not full F01 closure: named-issuer status, national legal rules, historical timeline quantities and primary-source locators for numerous chapters remain open. Editors 02–04 completed focused propagation passes in owned prose, with one final After Gold editorial correction during package 10's targeted fixes. Package 05 split bundled events and regenerated final coverage/status for **421 durable rows: 342 checked chapter destinations, 79 reviewed source-timeline fallbacks and zero pending destinations**. These reviewed links do not certify all row dates, quantities or sources; 05 lists remaining unknowns. The cross-volume five-minute synthesis remains withheld: the 02–04 exact conceptual reviews now converge on scoped copy, and package 08 prepared paragraph-level source/claim relationship data, but publication and route gates remain. The Takeaways route browses the 32 approved chapter answers by volume/topic instead of concatenating unreviewed chapter takeaways; its filters are encoded in the hash route. Its component keeps these volume/topic views visible even if a later accepted synthesis is supplied. Comparison publishes five scoped cases: Monzo transfer, Bitcoin main-chain transfer, Bank of England note household offer and merchant acceptance choice, plus one partial Circle USDC contract-rights case; all other cells remain pending. Chart candidates remain withheld with per-candidate data/reuse gates.

An accepted Rosstat historical CPI source/claim now gives After Gold 04's 1992 Russia figure its exact December 1991–December 1992 denominator and 2,508.8% rise; the 2 January liberalisation policy event stays separate. The original Rosstat page and IMF PDF were unavailable to an automated open even though official search records exposed the named values, so package 10 must check reader-facing source reachability. Editor 03 is scanning remaining ruble-zone/1998–2000 causal wording; package 05 owns a separate chronology row if needed.

After the first full editor/timeline freeze, the coordinator refreshed 44 manifest word/H2 records with `scripts/refresh-manifest.mjs --write`, preserving changed-title and old-section aliases. Package 10's first consolidated build exposed two retired Bitcoin-04 CAR section IDs in 05-owned direct timeline references; 05 patched both and regenerated status/coverage. After Gold 08/10's final causal/IMF-scope corrections were merged with an old-section alias and targeted metadata refresh. The final content build generated 44 article pages, 128 glossary terms and a sitemap, and the updated local suite passed 73/73. A later coordinator-owned mobile Menu Escape fix rebuilt and passed 10/10 affected route/static checks with live focus retest. [The release decision](RELEASE-DECISION.md) keeps this a staged local preview because source/editorial, human, accessibility, deployed and field gates remain. Verified human moderation, attribution/contact (contrary to the user's source-page-only preference), deployed indexing/social fetch and field performance cannot be inferred from local checks.

Deferred package-10 cases: all 32 R08 records should render question/answer, three-to-five takeaways and evidence/limits consistently in React and static HTML, including Gold-02/09 and Bitcoin-10 corrections; Takeaways volume/topic hash filters and browser Back should restore the same approved-answer set; accepted chapter claim locator edits should propagate to both outputs, while review-state claims remain absent; five accepted comparison cells should retain exact source/claim matches and pending rows should stay visibly pending; the IMF FRY PDF currently returned a 403 to one automated web fetch, requiring a reader-facing reachability check; and all timeline status IDs/routes/aliases should match the final row registry. These are final-pass checks, not editing gates.
