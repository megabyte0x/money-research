# Release quality gates — final edited local checkpoint, 15 September 2026

This records the final integrated **421-row / 421-reviewed** local edit freeze. The coordinator regenerated timeline coverage/status and refreshed manifest metadata. The production build generated **44 article pages**, **128 glossary terms** and a sitemap. After updating stale test fixtures to the accepted editorial contracts, the complete local suite passed **73/73** tests on that built output. All 421 timeline destinations have individual relevance/chronology dispositions: **342 checked chapter destinations** and **79 reviewed source-timeline fallbacks**; this does not certify every event's factual accuracy. Thirty-two approved conceptual chapter summaries, 64 exact-section next steps and five scoped comparison cells are integrated. These are structural/copy checks, not source-accuracy or release sign-off. The acceptance targets below come from `website-improvement-plan.md` §9.

| Acceptance target | Current evidence | Release gate |
|---|---|---|
| 1. Comprehension | No moderated reader sessions performed. | Five real sessions; at least four readers distinguish reserve asset, medium of exchange and unit of account. |
| 2. Discovery | No moderated reader sessions performed. | At least four of five readers complete the two specified discovery tasks within two minutes each. |
| 3. Evidence | Structured registry has **37 accepted sources and 23 accepted claims** with exact supporting locators; ten dated observations are separately typed. The original seven-source URL review opened 7/7 URLs (six exact locator sets visible, ECB Chart 7 panel-a partial). A [new bounded eight-source check](FINAL-SOURCE-REACHABILITY.md) opened Treasury/PCAOB/Bank of England/U.S. Mint/Bitcoin Guide, while Rosstat CPI failed certificate validation in the reader browser and automated fetch; two IMF historical PDFs returned 403 to automated fetch. Five comparison cells have source-matched claims; other cells and all eleven chart candidates remain pending/withheld. | Finish the 37-source reader-facing URL/locator check, clarify inaccessible citation alternatives without bypassing warnings, and dispose remaining priority chapter figures/current legal notes and comparison cells. No chart is published. |
| 4. Consistency | The final edited production build passed; all **73/73** local tests now pass. Tests cover selected scope regressions, summary/static parity, source links, five comparison cells, all stable timeline destinations and generated routes. Thirty-two summaries and 64 next steps render in React/static HTML; copy approval remains distinct from whole-chapter figure/legal sign-off. | Continue the recorded F01 source/claim propagation audit for historical superlatives, 2025–26 figures, named issuers and legal rules. A passing test is not editorial certification. |
| 5. Navigation | Build generated 44 direct article pages and the declared Bitcoin numeric aliases. Structural tests cover all direct paths, aliases and section links; **421/421** timeline rows have reviewed destinations or explicit fallbacks (**342/79**). [Bounded local production browser checks](FINAL-BROWSER-CHECK.md) covered Gold/After/Bitcoin direct loads, a copied Gold section, Bitcoin 13 numeric alias, old Bitcoin 04 section hash, Search filter/zero-result/Back and seven connected views. | Broader manual route matrix and independently audited primary-source dates/quantities even on reviewed timeline rows. |
| 6. Responsive behavior | Earlier spot checks covered 320/390/768 px selected views in both themes. At the final build's 320 px [production check](FINAL-BROWSER-CHECK.md), Comparison document width was 305/320 px in dark and light, and its 650 px table scrolled within a 273 px local container. Light-theme Bitcoin 13 alias also fit at 305/320 px. | Complete full route/width/touch matrix at 320/390/768 px and desktop in both themes; these bounded checks establish only the named views. |
| 7. Accessibility | Earlier light-theme glossary Escape restored focus and [token-pair contrast spot check](../00/CONTRAST-CHECK.md) gave revised light muted text 5.31:1 base, 4.72:1 soft and 4.74:1 mark. A final 320 px Menu keyboard defect was fixed under App ownership: Escape now closes the Menu, returns focus and retains a visible solid outline; the Comparison Use select showed a 2 px visible focus outline. Static tests confirm headings and link targets, but these do not certify component contrast, full keyboard order or screen-reader behavior. | Formal automated and human keyboard/screen-reader review in both themes against current official WCAG AA criteria. |
| 8. Performance | Post-Menu Vite build reports JS **258.68 kB raw / 79.84 kB gzip** and CSS **17.64 kB raw / 3.78 kB gzip**. Final content index is **953,350 uncompressed bytes**. One [loopback HTTP observation](FINAL-BROWSER-CHECK.md) fetched it with curl 8.22.0 (HTTP 200, first byte 0.001292 s, total 0.002366 s); no device/network throttling, browser Performance API or CWV trace tool was available. Earlier [HTTP observations](LAB-HTTP-OBSERVATION.md) were on older builds. | Controlled browser FCP/LCP/CLS and interaction delay on representative routes; deployed field p75 LCP ≤2.5 s, INP ≤200 ms, CLS ≤0.1 separately. |
| 9. URLs/metadata | Route/static tests pass unique title/description, canonical/OG identity, declared alias parity and social-image asset checks. The final bounded local browser check confirmed Bitcoin 13 numeric alias kept the canonical chapter URL, an old Bitcoin 04 section hash resolved, and Search Back restored a filter URL. | Broader browser route matrix; deployed responses/social fetch and indexing separately. |

Remaining first-release dependencies are specific: dispose priority editorial claims across repeated views; accept the withheld short synthesis only after its comparison/source gates; complete missing arrangement comparison cells and dated glossary notes; independently audit uncertain chronology/quantities on all 421 rows even though destination relevance is reviewed; keep all eleven chart candidates explicitly withheld; finish live route/viewport and formal accessibility matrices; measure controlled browser lab and deployed field performance; check deployed responses, source locators, social fetch and indexing; assign dated-data and privacy-measurement owners/retention; and run five real moderated sessions. The user's methods/about preference supplies source-page URL links only, so original F07 attribution and public corrections-contact targets remain unresolved rather than fabricated.

## Reproducible local checks

Run `npm run test:release` after editors and the coordinator freeze the manifest and shared model. It builds fresh output, then runs every test through `npm test`. The first final edited run built successfully and found ten stale deferred test assertions (63/73); after focused contract updates `npm test` passed 73/73 on that fresh output. A later coordinator-owned App Menu Escape fix prompted `npm run build` and the affected route/static tests (10/10 passed), plus a live keyboard retest. No content changed between the 73/73 suite and targeted App rebuild.

The initial baseline reported **56 tests, 46 pass, 10 fail**. Earlier historical freezes reached 60/60, 63/63 at 318 rows, and 72/72 at 337 rows. The final 421-row build generated **44 article pages and 128 glossary terms**. Its first suite run found 10 stale fixture/expectation failures after accepted summaries, five comparison cells and the complete timeline review; package 10 updated those assertions to the reviewed contracts, including date/scope checks, and reran the suite on the just-built output: **73/73 passed**. Gold 07's conditional Triffin mechanism, After Gold 07's bounded opening, After Gold 12's scoped fiat definition and seven reviewed conceptual glossary definitions remain bounded editorial changes; full chapter/source accuracy remains open. Browser evidence is limited to routes, viewports and interactions specifically named in this matrix.

## Browser and external checks to record at execution

For each browser run, record build commit/state, browser/version, route, viewport, theme, JavaScript state and result. Use representative direct loads from each volume, a numeric alias, `#/volume/slug/section`, `?section=`, copied section link, Back, search query/filter/no-results URLs, and no-JavaScript source HTML. Compare the heading and section target before and after hydration. Route matrix output should include all 44 canonical paths and their aliases.

The bounded accepted-registry check is documented in `ACCEPTED-SOURCE-URL-CHECK.md`: 7/7 URLs opened, 6/7 locator sets were directly visible and ECB graphic-column inspection remains partial. For later external checks, record source ID, exact URL, timestamp, response/redirect chain and whether the documented locator is visible. Classify unavailable responses for editorial follow-up; a network error does not establish that the underlying claim is false. Check a deployed preview's canonical and social response separately from local `dist`. Sitemap validity alone does not establish indexing.

For lab performance, record device profile, network throttling, warm/cold cache, browser/tool version, route, build state, LCP and CLS; report interaction-specific lab delay separately from field INP. Measure home, one chapter per volume, search, timeline, glossary, comparison and mechanics. Investigate the content-index transfer and shared bundle only from measured load costs.
