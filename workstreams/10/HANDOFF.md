# Package 10 handoff — final edited local checkpoint, 15 September 2026

The final edited production build generated **44 article pages**, **128 glossary terms** and a sitemap. The first consolidated suite run was 63/73: ten tests still expected old summary, comparison, timeline or wording contracts. Package 10 updated its owned test fixtures and kept event-specific and scope assertions; the rerun of `npm test` on the newly built output passed **73/73**. The timeline has **421/421 individual destination/relevance reviews**, comprising **342 checked chapter destinations** and **79 reviewed source-timeline fallbacks**. There are **37 accepted source records**, **23 accepted claims**, ten typed observations, 32 approved chapter summaries, 64 next steps and five comparison cells. This is a local structural/copy checkpoint, not editorial certification or release approval. [The gate matrix](QUALITY-GATES.md) records each acceptance target and [the bounded source check](FINAL-SOURCE-REACHABILITY.md) records new source failures.

## Owned changes

- `tests/release-routes.test.mjs`: a new build-output gate for 44 canonical destinations, numeric aliases, unique title/description/canonical/OG identity, no-JavaScript chapter body, section links and social-image asset coverage.
- `workstreams/10/QUALITY-GATES.md`: nine-target matrix of actual evidence and outstanding release inputs; reproducible test/browser/performance check requirements.
- `workstreams/10/OPERATIONS.md`: internal correction fields, triage, dated-data review triggers and minimal aggregate measurement contract. It follows the user's choice to show only source-page URLs on methods/about, with no attribution or public corrections contact.
- `workstreams/10/ACCEPTED-SOURCE-URL-CHECK.md`: bounded read-only publisher URL and exact-locator spot check for seven accepted registry sources.
- `workstreams/10/LAB-HTTP-OBSERVATION.md` and `perf-http-samples.tsv`: isolated local-preview HTTP timings and payload-size facts, with browser Core Web Vitals explicitly unavailable.
- Final package-10 test updates in `tests/article-metadata.test.mjs`, `tests/comparison-cells.test.mjs`, `tests/content.test.mjs`, `tests/evidence.test.mjs`, `tests/static-output.test.mjs` and `tests/timeline-coverage.test.mjs`: accepted contract assertions retain date/scope, executed-versus-announced auction, draft BIP non-activation, locator, comparison and static-summary checks.
- `workstreams/10/FINAL-SOURCE-REACHABILITY.md`: bounded high-priority source check; Rosstat CPI URL failed certificate validation, and IMF FRY/Pamphlet45 PDFs returned 403 to automated web fetch. No browser security warning was bypassed.
- `workstreams/10/FINAL-BROWSER-CHECK.md`: representative direct/alias/old-section, Search Back/zero-result and connected-view checks on the final local production preview; 320 px dark/light Comparison, local table scroll and Menu Escape/focus retest. One loopback HTTP payload sample is labeled as such, not as Core Web Vitals.

## Source evidence and results

Source specification: `plans/README.md`, `plans/10-release-quality.md`, `website-improvement-plan.md` §§7–10; active `package.json`, `index.html`, `scripts/prerender.mjs`, `tests/static-output.test.mjs`, manifest and current `dist`.

The initial baseline exposed duplicate cross-volume Glossary/Sources titles and missing `og:image`; the coordinator fixed both under shared-file lease. **Historical checkpoint:** the 318-row / 200-reviewed tree built 44 pages and passed 63/63 tests, with 118 row destinations then pending. That is superseded by the 421-row / 421-reviewed, 73/73 final edited checkpoint above. The previous local browser check covered Home, Compare, Paths, History, Sources, direct After Gold 09 with a section hash, a Bitcoin 13 summary and selected dark/light widths; it was not a complete route/accessibility matrix. The prior seven-source review opened 7/7 URLs, with six locator sets directly visible and ECB Chart 7 panel-a graphic partial. Older index sizes of 590,698/655,581 bytes are historical payload observations, not current performance results. No deployed preview, indexing check or human reader session has been performed.

## Shared-file status and remaining coordinator edits

1. `package.json` now has a full-suite `npm test` and fresh-build `npm run test:release`; prerender titles/descriptions and the local social-preview asset now pass the owned route gate. Deployed preview crawler behavior remains unchecked.
2. The direct-article routing fix was retested for After Gold 09 with a section hash. Run the broader alias, legacy hash, copied section, Back and search-state browser matrix before release.
3. The final 421-row edit freeze built successfully. After the initial 63/73 suite failures and targeted package-10 test changes, the full suite passed **73/73** on the just-built output. No content edit occurred between that build and passing suite. A subsequent mobile Menu Escape defect was fixed by the coordinator in App; the post-fix production build generated 44 pages and 128 glossary terms, its ten affected route/static tests passed 10/10, and the 320 px browser retest confirmed closed Menu, returned visible focus and no page-wide overflow.
4. Keep methods/about at source-page URL links only, as requested. A public corrections-contact gate from the original backlog remains unmet; do not fabricate contact, author or editor details.

## Remaining dependencies

Separate primary-source/date/quantity/composite audit (including reviewed rows), broader live route and both-theme viewport matrix, formal accessibility review, complete 37-source URL/locator check, controlled browser FCP/LCP/CLS lab (Performance API and CWV trace tool unavailable in this browser surface) and deployed field p75/INP performance, deployed social/indexing evidence, remaining comparison/evidence cells and current legal notes for seven accepted glossary cores, analytics deployment owner/retention, correction process owner and five real moderated sessions. Feature owners fix defects in their UI; the coordinator updates global status and release claims.
