# Bounded final production-browser check — 15 September 2026

Build: final edited local checkout based on `7c9939c`, with uncommitted package edits; Vite 5.4.21 production output from the 421-row manifest. Browser: Codex in-app browser on local `vite preview` at `http://127.0.0.1:4174/` (browser engine/version not exposed by the available tool). This is a representative route/interaction check, not a 44-page manual matrix or a screen-reader audit.

| Check | Actual observation |
|---|---|
| Direct chapter/section | Gold 05 copied `?section=the-gold-silver-ratio-through-time` loaded the Gold 05 H1 and hydrated at the exact H2 (`scrollY` 3209.5 after the page settled). After Gold 07 loaded its current chapter H1. Bitcoin 13 canonical `13-is-bitcoin-the-answer` loaded its current H1. |
| Numeric alias | `/bitcoin/13/` loaded the same Bitcoin 13 H1 and retained the canonical URL ending `/bitcoin/13-is-bitcoin-the-answer/`. Bitcoin numeric aliases are the active aliases in the current manifest; `/gold/05/` is **not** a declared alias and was not counted as an alias check. |
| Legacy section hash | `/#/bitcoin/04-countries-that-adopted-bitcoin-and-what-happened/central-african-republic-the-second-experiment-in-twelve-mon` displayed Bitcoin 04 and scrolled to the new “reported policy and legal questions” H2 (`targetTop` about 117 px after settling). |
| Search state/Back | A zero-result URL `#/search?q=xyz-no-result&volume=bitcoin` displayed 0 matching sections. `#/search?q=gold&volume=gold` displayed 80 matching sections; after navigating to the Bitcoin filter and pressing browser Back, the Gold-filter hash and same count returned. |
| Connected views | Research, Paths, Timeline, Glossary, Comparison, Mechanics and Takeaways each showed their expected H1 when loaded directly by hash. The connected Timeline view displayed 107 selected turning points from the underlying 421 source rows; this view count is not the source-row registry count. |
| No-JavaScript/static parity | Generated-output tests inspected all 44 direct HTML pages, their declared aliases, crawlable section targets and source/summary rendering. They confirm a static chapter body and canonical/OG metadata without relying on hydration; the browser was not switched to JavaScript-disabled mode. |
| 320 px both themes | Comparison document `scrollWidth` was 305 px at `innerWidth` 320 in both dark (body background `rgb(22, 21, 17)`) and light (`rgb(246, 243, 236)`) themes. The 650 px comparison table remained in a 273 px local scroll container, with no page-wide overflow. The Bitcoin 13 numeric alias in light theme also stayed at 305/320 px. |
| Mobile keyboard | The initial 320 px Menu check found Escape left it expanded. The coordinator added an Escape handler under App ownership; a refreshed production build retest showed `aria-expanded=false`, focus returned to the Menu button and the focused control had a visible solid outline. A later Tab onto the Comparison Use select also had a 2 px solid `:focus-visible` outline. |

After the Menu fix, `npm run build` regenerated 44 pages, 128 glossary terms and the sitemap, and the 10 affected route/static tests passed 10/10. The complete 73/73 suite had passed on the immediately preceding content-equivalent build; it was not needlessly rerun after this isolated App change.

The browser tool's read-only page evaluation did not expose the Performance API, so this pass did not produce FCP/LCP/CLS or interaction-specific browser delay. `curl` 8.22.0 made one unthrottled loopback fetch of the final content index: HTTP 200, first byte 0.001292 s, total 0.002366 s and 953,350 downloaded bytes. This single local response is a payload/timing observation, not a controlled browser lab or deployed Core Web Vitals result. The final asset files are 259,071 bytes JS and 17,637 bytes CSS; Vite reported 79.84 kB JS gzip and 3.78 kB CSS gzip. Controlled lab traces, deployed p75/INP, indexing/social fetch and human accessibility sessions remain separate gates.
