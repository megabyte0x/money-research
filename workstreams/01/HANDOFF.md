# 01 evidence seam handoff (in progress)

Coordinator-held shared lease. Starting drafts `public/content/sources.json` and `src/sources.js` were preserved and connected, with nullable unknown publication dates supported. Added `public/content/claims.json` for accepted E04/E10, `sourceId` relationships for five existing observations, and `src/evidence.js` validation. The active build now reads these records and emits sources, accepted claims, and article-specific dated-evidence locators in the content model. Static article generation renders locators for chapters containing observation tokens; the Vite watcher rebuilds on registry edits.

Existing E04/E10 observations and locators come from the pre-existing local `EDITORIAL-SOURCES.md` ledger and five previously reviewed observation records. This pass did not independently recheck the publisher pages. No new empirical observation was approved. Four source records are mapped to five observations; other major claims remain outside the structured registry pending packages 02–04. Accepted `reviewedBy` currently records the prior ledger process, not a verified named editor.

`node --test tests/evidence.test.mjs` passes. Full build and static-output suite await stable chapter writes and manifest regeneration; no browser check has been performed for this seam. Migration procedure: preserve stable IDs; add each accepted source with real or null publication date, append claim with exact supporting/contrary locators and review fields, link observation by `sourceId` and accepted `claimId`, then run `npm run build` and `node --test --test-isolation=none tests/*.test.mjs`. Generated `index.json`, `resolved/` and `dist/` are build outputs only.

The coordinator subsequently rechecked the WGC FY2025 Table 1 and Q1 2026 erratum, added two WGC source records, two scoped E29 claims and five category/revision observations, and replaced repeated Gold literals with observation tokens. The FY2025 `Gold Demand` (ex-OTC), `Total Demand`, jewellery consumption and fabrication rows are separate; the Q1 erratum supersedes a still-visible older 244-tonne figure on its page. These records do not certify other Gold demand figures or charts.

The coordinator subsequently added a validated `article-metadata.json` seam. Editor-approved conceptual question/answer copy and exact-section next steps for all 32 topic chapters now render in React and static HTML. This is copy/routing approval only; the existing reader notice keeps dated chapter claims under review. Five guided paths are integrated from the discovery catalog.

Still needed: integrate remaining editor claim packets, populate comparison records with accepted arrangement-specific evidence, audit canonical glossary terms with 08, expose broader reader citations, verify cross-view value propagation, and reconcile the plan's attribution criterion with the user's source-page-only preference. Infrastructure alone does not close F03/F04 or the release gate.

The coordinator has now added `articleClaims` output for accepted records that explicitly list dependent article IDs. React and static pages render each scoped assertion with its registered source URL and exact supporting locator. Review-state and withheld claims remain excluded by `createEvidenceIndex`. Package 10 should verify that changing an accepted source/locator propagates to generated index, static HTML and the hydrated article, and that a missing/invalid source relationship fails publication. This is a narrow citation path; the remaining claim packets and chapter-wide source audit are still open. Editing-wave testing remains deferred.
