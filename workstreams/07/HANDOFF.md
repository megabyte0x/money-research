# Package 07 reader handoff — 15 September 2026

## Implemented within reader ownership

- Added `src/features/reader/ReaderViews.jsx` and scoped `reader.css`. These export `HomePage`, `MethodsPage`, and `ArticlePage`, each receiving the already-derived `v` prop. They do not fetch, parse routes or mutate content records.
- HomePage gives reading actions, four functions of money, question cards, qualitative custody preview, overlapping history, three volume slots, evidence/uncertainty distinction and a methods link. Its volume cards are data driven via `v.homeVolumes`; until that prop is supplied, the section renders no volume cards and should not replace the inline home.
- ArticlePage renders existing Markdown body, mobile section/chapter selector, metadata, evidence notice, adjacent chapters and file references. For topic chapters it accepts an approved `v.articleSummary` and `v.articleLinks`; no unreviewed editorial packet is copied into JSX. Reference records omit the topic summary through `v.articleIsReference`.
- MethodsPage describes scope and source-page process. Following the user's answer, it has no author/editor names, roles, corrections contact, or outbound source URLs. External source links remain on each volume's source page.
- `src/MoneyMechanics.jsx` adds the missing simplified bond balance-sheet table. Every mechanics table is a keyboard-focusable named scroll region, so the horizontally scrollable table can be inspected at narrow widths. The four scenarios retain distinct deposit, reserve, bond and QE changes.
- Added isolated `src/features/reader/HistoryView.jsx` with the eleven existing deep-link IDs (`arc-1` through `arc-9`, plus `arc-interwar` and `arc-digital`) and ordered `data-stage` values for App's stage/progress observer. At mobile width it renders a descriptive regime-and-date selector; at larger widths it renders a scrollable stage link band. Each stage has anchor, power, change, scope/uncertainty and a checked related-chapter link. The interwar bridge and Bitcoin/stablecoin overlap stay distinct; the withheld-chart notice remains visible.

## Exact shared-file wiring requests for 00

1. Import `{ HomePage, MethodsPage, ArticlePage }` from `./features/reader/ReaderViews.jsx` in `src/App.jsx`. Replace the inline blocks gated by `v.isHome`, `v.isMethods`, and `v.isArticle` (including its separate mobile selector) with `<HomePage v={v} />`, `<MethodsPage v={v} />`, and `<ArticlePage v={v} />`. Keep shell route/history/selection ownership in App. The current `<main onMouseUp={v.onArticleMouseUp}>` handler already exists; avoid introducing a duplicate selection action when integrating.
2. Supply `v.homeVolumes` as three objects in Gold/After Gold/Bitcoin order: `{ id, label, href, title, question, commitment }`. Derive title/question/reading commitment from accepted directory/manifest metadata, and use the stable directory article href for each volume. Do not copy a disputed current figure into cards.
3. Supply `v.articleSummary` only from accepted generated topic metadata as `{ question, answer, takeaways: string[], evidence, uncertainty }`. Supply `v.articleLinks` as `{ href, kind, title, reason }[]` after the 02–04 typed section-link proposals are reviewed, target heading IDs are resolved and the 01 model has the fields. Set `v.articleIsReference` for volume directories, timelines, glossaries, source lists and other reference records. Existing body text remains visible even when metadata is absent.
4. The current inline Methods page contains an attribution paragraph and a GitHub correction issue link. Remove that block when replacing it with MethodsPage; the user requested no attribution/contact details and only source URLs on source pages. Keep source-page links in MethodsPage.
5. Import `HistoryView` from `./features/reader/HistoryView.jsx` and replace App's inline `v.isArc` block with `<HistoryView v={v} />`. Keep App's `ARC` array, `arcBand`, `toc`, `arcStage`, `currentStage`, sticky-header offset and route handling while integrating. The component's ordered `data-stage` values correspond to `App.ARC`; every `id` preserves existing `#/arc/<id>` deep links. The mobile selector navigates through those same hash routes. Check stage progress and header-adjusted deep-link scroll after extraction. The new copy is shorter and more qualified than the old arc; 02–04 should still review it against their final evidence packets. Coordinator/10 own browser route, focus and responsive checks after wiring.

## Evidence and dependencies

The home and mechanics language is qualitative and follows existing page text and the 02/03/04 packets' cautions: bank deposits/reserves/bonds are distinct; digital development includes Bitcoin and stablecoins; gold/fiat/bank claims coexist. The bond table is a simplified issuance position, not a full settlement-flow accounting. No current market, reserve or adoption numbers were added.

The 02–04 summary and typed-link packets are proposals rather than merged metadata, so `v.articleSummary` and `v.articleLinks` must remain absent until 01/00 acceptance. History accuracy/caption revisions and eleven withheld charts depend on 02–06. Canonical glossary/touch behavior depends on 08 and App's inline renderer. Verified human authorship and corrections workflow were unavailable; the user's explicit preference supersedes the plan's attribution/contact request.

## Checks actually performed

`node_modules/.bin/esbuild src/features/reader/ReaderViews.jsx --bundle --external:react --outfile=/tmp/money-reader-check.js` passed, including JSX and scoped CSS parsing. No integrated Vite build, test suite, browser flow, responsive screenshot, touch or screen-reader check was performed by package 07. Those are pending App integration and 10's quality pass.

Follow-up checks: `node_modules/.bin/esbuild src/features/reader/HistoryView.jsx --bundle --external:react --outfile=/tmp/money-history-check.js` passed JSX/CSS parsing. A manifest slug check found eleven stage chapter links and zero unmatched targets. After coordinator integration, HomePage receives all three `homeVolumes` objects and ArticlePage receives `articleIsReference`; ArticlePage has no nested `onMouseUp` handler, so the shell's selection handler remains single. The accepted topic summary/link fields are still absent in App, so no summary or curated next-step copy appears yet. No browser or assistive-technology check was performed by 07.
